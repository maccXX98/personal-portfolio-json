---
import Section from "@/components/section.astro";
import { work } from "@cv";
---

<Section title="Experience">
  <ul>
    {
      work.map(({ name, startDate, endDate, position, summary, highlights }) => {
        const startYear = new Date(startDate).getFullYear();
        const endYear = endDate ? new Date(endDate).getFullYear() : "Actual";
        const years = `${startYear} - ${endYear}`;

        return (
          <li>
            <article>
              <header>
                <div>
                  <h3>{name || "Sin nombre"}</h3>
                  <h4>{position || "Sin posición"}</h4>
                </div>
                <time>{years}</time>
              </header>
              <footer>
                <p>{summary || "Sin resumen disponible."}</p>
                {highlights && highlights.length > 0 && (
                  <ul>
                    {highlights.map((highlight) => (
                      <li>{highlight}</li>
                    ))}
                  </ul>
                )}
              </footer>
            </article>
          </li>
        );
      })
    }
  </ul>
</Section>

<style>
  ul {
    display: flex;
    flex-direction: column;
    gap: 32px;
    padding: 0;
    list-style: none;
  }

  article h3 {
    font-weight: 500;
    color: #111;
    margin: 0;
  }

  article h4 {
    font-weight: 400;
    color: #222;
    margin: 4px 0;
  }

  header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 4px;
  }

  time {
    font-size: 0.85rem;
    color: #666;
  }
</style>
